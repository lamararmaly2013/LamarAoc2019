<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="y2019.aoc.lamar.lamaraoc2019">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomePageEActivity"></activity>
        <activity android:name=".CheckBoxListActivity" />
        <activity android:name=".AddExamActivity" />
        <activity android:name=".AddHomeWorkActivity" />
        <activity android:name=".ReminderActivity" />
        <activity android:name=".ExamsActivity" />
        <activity android:name=".HomeWorkActivity" />
        <activity android:name=".ScheduleActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".HomePageActivity" />
        <activity android:name=".LogInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>